<% include ../../partials/head.ejs %>
<% include ../../partials/nav %>
<div class="row bg-grey">
    <div class="container">
        <div class="col-md-10 offset-md-2">
            <% include ../../partials/no-tabs %>
        </div>
    </div>
</div>
<div class="container">
    <div class="row">
        <% include ../../partials/sidebar %>

        <div class="col-md-10">
            <main>
                <% include ../../partials/dashboard/controller %>
                <% gifts.forEach((gift) => { %>
                <div class="row">
                    <div class="col-md-12">
                        <span><%= gift.giftNumber %></span>
                        <span><%= gift.giftAmount %></span>
                        <span><%= gift.giftDescription %></span>
                        <span><%= gift.giftAmount %></span>
                        <span><%= gift.giftCode %></span>
                        <span><%= gift.senderFirstName %></span>
                        <span><%= gift.senderLastName %></span>
                        <span><%= gift.giftMessage %></span>
                    </div>
                </div>
                <% }) %>
            </main>
        </div>
    </div>
</div>
<% include ../../partials/contact %>
<% include ../../partials/footer %>
